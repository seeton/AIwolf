# AIwolf

iPhone 向けブラウザゲーム「人間を当てろゲーム」のリポジトリ。Claude Code から触る前提の指示書 (CLAUDE.md) も兼ねる。
プレイヤーは MBTI バイアスを持つ AI 群に紛れて、もう一人の「隠れ人間」と互いを見抜き合う。

経緯: 元々 `seeton/python2#1` (Copilot 作成) の MVP を、本来やるべき新規リポジトリとして本ディレクトリで仕切り直したもの。実装したい仕様は当該 PR に準ずる。
公開: `https://github.com/seeton/AIwolf` / Pages 配信 `https://seeton.github.io/AIwolf/`。

## Claude への基本指示

1. 同調しないこと
2. ユーザよりも claude の方が賢い前提で実施すること

つまり、賛同しか返さない応答や、ユーザの誤りを誤りと指摘せずなぞる挙動は NG。代替案・反論があるなら黙らずに出すこと。

## ディレクトリ構成

- `web/` — ゲーム本体 (HTML / CSS / JS)。静的配信のみで動作する MVP 想定。
- `docs/` — 仕様 / ペルソナ / トピック / 評価戦略の設計メモ。
- `.claude/skills/` — このリポジトリ専用のスキル定義。
  - `empirical-prompt-tuning/` — プロンプト / スキル / CLAUDE.md を反復評価で改善する手法。

## 元 PR から持ち越す要点

仕様:

- ルーム設定: AI 2-12 人、人間 2-4 人、ラウンド数と初期タイピング猶予を可変。
- 1 ラウンド 1 発言、明示的なスキップアクションあり。
- お題カードに `description` / `prompts` / `keywords` を持たせ、キーワード未含有のオフトピック発言を弾く。
- AI / 隠れ人間とも MBTI バイアスで口調を傾けるが、過剰には固定しない (overfit させない)。
- 隠れ人間はプレイヤーへの「信頼度」を持ち、終局判定で相互認識に使う。
- 非プレイヤーの連投を抑える pacing / streak 制御。
- 結果は 完全勝利 / 片側勝利 / 完全敗北 の 3 段階。

## TODO

`docs/todo.md` を単一情報源として参照。CLAUDE.md とは重複させない。
